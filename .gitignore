# Raw + generated data are not versioned; reproduce via src/00_download_data.py
data/raw/*.csv
data/raw/*.zip
data/processed/*
*.duckdb
*.duckdb.wal

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Power BI
*.pbix.bak
~$*
# Desktop writes these when a .pbip is opened. cache.abf is a full copy of
# the imported data; localSettings.json is per-machine. Neither is source.
**/.pbi/localSettings.json
**/.pbi/cache.abf

# OS / editor
Thumbs.db
.DS_Store
.vscode/
.idea/

# Generated analysis result sets (regenerate: python src/04_run_analysis.py)
data/processed/analysis/
